#!/usr/bin/env bash
DIR="$(cd "$(dirname "$0")" && pwd)"
if [ ! -x "$DIR/.venv/bin/python" ]; then
  echo "No virtualenv yet. Run:  ./setup.sh"
  exit 1
fi
exec "$DIR/.venv/bin/python" "$DIR/runner/cli.py" "$@"
